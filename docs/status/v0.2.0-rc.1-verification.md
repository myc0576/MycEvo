# v0.2.0-rc.1 verification

The clean release clone passed the full public test suite (`454 passed,
2 skipped`), package build, `twine check`, Shortcraft validation, Gitleaks
8.30.1 scan, and default-deny public release audit (`ok=true`).

The real golden path used the persistent Docling 2.116.0 Python runtime and
Typst 0.15.1 CLI. It produced a non-empty PDF, IR, checksums and provenance
through the constrained runner. No PyPI, Docker or Marketplace publication
was performed.
