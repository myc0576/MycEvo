# Corpus Factory and Shortcraft V1

Corpus providers are license-first and plugin-bound. `LocalPrivateProvider` is
offline-only and cannot upload or populate public fixtures. Shortcraft cards are
candidate artifacts validated against `shortcraft/schemas/card.v1.schema.json`;
rendered galleries are generated from project code and synthetic data only.
